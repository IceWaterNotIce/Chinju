.distance-marker {
    flex-direction: row;
    align-items: center;
}

.distance-line {
    height: 4px;
    background-color: #fff;
    margin-right: 8px;
    flex-grow: 1;
    border-radius: 2px;
}

.distance-label {
    color: #fff;
    font-size: 16px;
    font-weight: bold;
}
